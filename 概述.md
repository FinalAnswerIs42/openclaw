# 仓库指南

- 仓库地址：https://github.com/openclaw/openclaw
- 在聊天回复中，文件引用必须只使用仓库根目录相对路径（示例：`src/telegram/index.ts:80`）；不要使用绝对路径或 `~/...`。
- 不要编辑受安全导向 `CODEOWNERS` 规则覆盖的文件，除非列表中的 owner 明确要求修改，或已在审阅你的改动。把这些路径视为受限区域，而不是顺手清理目标。

## 项目结构与模块组织

- 源代码：`src/`（CLI 接线在 `src/cli`，命令在 `src/commands`，web provider 在 `src/provider-web.ts`，基础设施在 `src/infra`，媒体流水线在 `src/media`）。
- 测试：与源文件同目录放置 `*.test.ts`。
- 文档：`docs/`（图片、队列、Pi 配置）。构建产物在 `dist/`。
- 术语：在文档、UI、变更日志和贡献者指引中使用 “plugin” / “plugins”。为避免全仓库重命名带来的变动，内置 workspace 插件树仍保留当前内部包布局。
- 内置插件命名：对于仓库自有的 workspace 插件，需保持规范插件 id 在 `openclaw.plugin.json:id`、默认 workspace 文件夹名、以及以同一 id 为锚点的包名之间一致（`@openclaw/<id>` 或批准的后缀形式，如 `-provider`、`-plugin`、`-speech`、`-sandbox`、`-media-understanding`）。保持 `openclaw.install.npmSpec` 等于包名；若存在 `openclaw.channel.id`，其值也要等于插件 id。例外必须明确并由仓库不变量测试覆盖。
- 插件：位于内置 workspace 插件树（workspace packages）中。仅插件使用的依赖放在对应扩展的 `package.json`，除非核心也使用，否则不要加到根 `package.json`。
- 插件安装：会在插件目录执行 `npm install --omit=dev`；运行时依赖必须放在 `dependencies`。避免在 `dependencies` 中使用 `workspace:*`（会导致 npm install 失败）；把 `openclaw` 放在 `devDependencies` 或 `peerDependencies`（运行时通过 jiti alias 解析 `openclaw/plugin-sdk`）。
- 导入边界：扩展生产代码应把 `openclaw/plugin-sdk/*` 与本地 `api.ts` / `runtime-api.ts` barrel 作为公共接口。不要直接导入核心 `src/**`、`src/plugin-sdk-internal/**` 或其他扩展的 `src/**`。
- 由 `https://openclaw.ai/*` 提供的安装脚本：位于同级仓库 `../openclaw.ai`（`public/install.sh`、`public/install-cli.sh`、`public/install.ps1`）。
- 消息渠道：重构共享逻辑（路由、allowlist、配对、命令门控、引导、文档）时，始终考虑**所有**内置与扩展渠道。
  - 核心渠道文档：`docs/channels/`
  - 核心渠道代码：`src/telegram`、`src/discord`、`src/slack`、`src/signal`、`src/imessage`、`src/web`（WhatsApp web）、`src/channels`、`src/routing`
  - 内置插件渠道：workspace 插件树（例如 Matrix、Zalo、ZaloUser、Voice Call）
- 新增渠道/插件/app/文档时，更新 `.github/labeler.yml` 并创建对应 GitHub labels（沿用现有渠道/插件标签颜色）。

## 架构边界

- 仓库地图从这里开始：
  - 内置 workspace 插件树 = 内置插件，以及最接近第三方插件的示例接口面
  - `src/plugin-sdk/*` = 扩展允许导入的公开插件契约
  - `src/channels/*` = 位于插件/渠道边界之后的核心渠道实现细节
  - `src/plugins/*` = 插件发现、manifest 校验、loader、registry、契约强制
  - `src/gateway/protocol/*` = 强类型 Gateway 控制平面与节点线协议
- 渐进式细节在本地边界指引中：
  - bundled-plugin-tree `AGENTS.md`
  - `src/plugin-sdk/AGENTS.md`
  - `src/channels/AGENTS.md`
  - `src/plugins/AGENTS.md`
  - `src/gateway/protocol/AGENTS.md`
- 插件与扩展边界：
  - 公共文档：`docs/plugins/building-plugins.md`、`docs/plugins/architecture.md`、`docs/plugins/sdk-overview.md`、`docs/plugins/sdk-entrypoints.md`、`docs/plugins/sdk-runtime.md`、`docs/plugins/manifest.md`、`docs/plugins/sdk-channel-plugins.md`、`docs/plugins/sdk-provider-plugins.md`
  - 定义文件：`src/plugin-sdk/plugin-entry.ts`、`src/plugin-sdk/core.ts`、`src/plugin-sdk/provider-entry.ts`、`src/plugin-sdk/channel-contract.ts`、`scripts/lib/plugin-sdk-entrypoints.json`、`package.json`
  - 规则：扩展进入核心只能通过 `openclaw/plugin-sdk/*`、manifest 元数据、以及文档化的运行时 helper。扩展生产代码不要导入 `src/**`。
  - 规则：核心代码和测试不得深度导入内置插件内部实现（如插件的 `src/**` 文件或 `onboard.js`）。如果核心需要某内置插件 helper，应通过该插件 `api.ts` 暴露；若为真实跨包契约，则通过 `src/plugin-sdk/<id>.ts` 暴露。
  - 兼容性：允许新增插件接缝，但必须以文档化、向后兼容、版本化的契约方式引入。已有第三方插件在使用，不能随意破坏。
- 渠道边界：
  - 公共文档：`docs/plugins/sdk-channel-plugins.md`、`docs/plugins/architecture.md`
  - 定义文件：`src/channels/plugins/types.plugin.ts`、`src/channels/plugins/types.core.ts`、`src/channels/plugins/types.adapters.ts`、`src/plugin-sdk/core.ts`、`src/plugin-sdk/channel-contract.ts`
  - 规则：`src/channels/**` 是核心实现。若插件作者需要新接缝，应加到 Plugin SDK，而不是让他们导入渠道内部实现。
- Provider/模型边界：
  - 公共文档：`docs/plugins/sdk-provider-plugins.md`、`docs/concepts/model-providers.md`、`docs/plugins/architecture.md`
  - 定义文件：`src/plugins/types.ts`、`src/plugin-sdk/provider-entry.ts`、`src/plugin-sdk/provider-auth.ts`、`src/plugin-sdk/provider-catalog-shared.ts`、`src/plugin-sdk/provider-model-shared.ts`
  - 规则：核心拥有通用推理循环；provider 插件通过注册和类型化 hooks 管理 provider 专属行为。不要通过深入无关核心内部来解决 provider 需求。
  - 规则：避免在无关核心代码中临时读取 `plugins.entries.<id>.config`。若核心需要插件拥有的鉴权/配置行为，应新增或复用通用接缝（`resolveSyntheticAuth`、公共 SDK/helper facade、manifest 元数据、插件自动启用 hooks），并遵守插件禁用状态与 SecretRef 语义。
  - 规则：厂商专属工具与设置应归属对应插件。除非工具有意属于核心，否则不要把 provider 专属工具配置、密钥收集或运行时启用逻辑加到核心 `tools.*` 面。
- Gateway 协议边界：
  - 公共文档：`docs/gateway/protocol.md`、`docs/gateway/bridge-protocol.md`、`docs/concepts/architecture.md`
  - 定义文件：`src/gateway/protocol/schema.ts`、`src/gateway/protocol/schema/*.ts`、`src/gateway/protocol/index.ts`
  - 规则：协议改动即契约改动。优先做增量演进；不兼容改动需要显式版本化、文档更新及客户端/代码生成联动。
- 内置插件契约边界：
  - 公共文档：`docs/plugins/architecture.md`、`docs/plugins/manifest.md`、`docs/plugins/sdk-overview.md`
  - 定义文件：`src/plugins/contracts/registry.ts`、`src/plugins/types.ts`、`src/plugins/public-artifacts.ts`
  - 规则：manifest 元数据、运行时注册、公开 SDK 导出、契约测试必须保持一致。不要绕过已声明插件接口创建隐藏路径。
- 扩展测试边界：
  - 尽可能把扩展自有的 onboarding/config/provider 覆盖放在其所属内置插件包下。
  - 若核心测试需要内置插件行为，应通过公开 `src/plugin-sdk/<id>.ts` facade 或插件 `api.ts` 消费，而不是私有扩展模块。

## 文档链接（Mintlify）

- 文档托管于 Mintlify（docs.openclaw.ai）。
- `docs/**/*.md` 内部链接：使用根相对路径，不带 `.md`/`.mdx`（示例：`[Config](/configuration)`）。
- 处理文档时，阅读 mintlify skill。
- 对文档、UI 文案和选择器列表，除非章节明确描述运行时行为（如自动探测或执行顺序），否则服务/provider 按字母序排列。
- 章节交叉引用：在根相对路径上使用锚点（示例：`[Hooks](/configuration#hooks)`）。
- 文档标题和锚点：避免在标题中使用破折号和撇号，否则会破坏 Mintlify 锚点链接。
- 当用户请求链接时，回复完整 `https://docs.openclaw.ai/...` URL（不要用根相对）。
- 修改文档时，回复末尾附上你引用过的 `https://docs.openclaw.ai/...` URL。
- README（GitHub）中保持绝对文档 URL（`https://docs.openclaw.ai/...`），确保在 GitHub 可用。
- 文档内容要通用：不要出现个人设备名/主机名/路径；使用 `user@gateway-host` 等占位符和 “gateway host”。

## 文档 i18n（zh-CN）

- `docs/zh-CN/**` 为生成产物；除非用户明确要求，否则不要编辑。
- 流程：更新英文文档 → 调整术语表（`docs/.i18n/glossary.zh-CN.json`）→ 运行 `scripts/docs-i18n` → 仅在被指示时做定向修复。
- 重新运行 `scripts/docs-i18n` 前，为任何新技术术语、页面标题或需保持英文/固定翻译的短导航标签（例如 `Doctor` 或 `Polls`）补充术语表项。
- `pnpm docs:check-i18n-glossary` 会在重跑翻译前，校验变更英文文档标题和短内部文档标签的术语覆盖。
- 翻译记忆：`docs/.i18n/zh-CN.tm.jsonl`（生成）。
- 参见 `docs/.i18n/README.md`。
- 流水线可能慢且效率不高；若拖慢明显，请在 Discord 联系 @jospalmbier，不要临时 hack 绕过。

## exe.dev VM 运维（通用）

- 访问：稳定路径是先 `ssh exe.dev`，再 `ssh vm-name`（默认 SSH key 已配置）。
- SSH 不稳定：使用 exe.dev web terminal 或 Shelley（web agent）；长任务保持 tmux 会话。
- 更新：`sudo npm i -g openclaw@latest`（全局安装到 `/usr/lib/node_modules` 需要 root）。
- 配置：使用 `openclaw config set ...`；确保 `gateway.mode=local` 已设置。
- Discord：只存原始 token（不要 `DISCORD_BOT_TOKEN=` 前缀）。
- 重启：先停旧 gateway，再执行：
  `pkill -9 -f openclaw-gateway || true; nohup openclaw gateway run --bind loopback --port 18789 --force > /tmp/openclaw-gateway.log 2>&1 &`
- 验证：`openclaw channels status --probe`、`ss -ltnp | rg 18789`、`tail -n 120 /tmp/openclaw-gateway.log`。

## 构建、测试与开发命令

- 运行时基线：Node **22+**（确保 Node 与 Bun 路径都可用）。
- 安装依赖：`pnpm install`
- 若依赖缺失（例如 `node_modules` 缺失、`vitest not found`、`command not found`），先执行仓库定义的包管理安装命令（优先 lockfile/README 指定 PM），再重跑一次原命令。适用于 test/build/lint/typecheck/dev；若重试仍失败，报告该命令及第一个可执行错误。
- pre-commit hooks：`prek install`。该 hook 会跑仓库验证流程，包括 `pnpm check`。
- `FAST_COMMIT=1` 仅会在 pre-commit 中跳过全仓库 `pnpm format` 与 `pnpm check`。用于你有意加速提交且已手动完成等价定向验证。它不会改变 CI，也不会改变 `pnpm check` 本身行为。
- 同时支持：`bun install`（涉及依赖/patch 变更时，保持 `pnpm-lock.yaml` 与 Bun patch 同步）。
- TypeScript 执行（脚本、开发、测试）优先 Bun：`bun <file.ts>` / `bunx <tool>`。
- 开发模式运行 CLI：`pnpm openclaw ...`（bun）或 `pnpm dev`。
- Node 仍支持运行构建产物（`dist/*`）与生产安装。
- Mac 打包（开发）：`scripts/package-mac-app.sh` 默认当前架构。
- 类型检查/构建：`pnpm build`
- TypeScript 检查：`pnpm tsgo`
- Lint/格式：`pnpm check`
- 本地 agent/dev shell 默认对 `pnpm tsgo` 与 `pnpm lint` 使用更省内存行为 `OPENCLAW_LOCAL_CHECK=1`；在 CI/共享运行中设 `OPENCLAW_LOCAL_CHECK=0`。
- 格式检查：`pnpm format`（oxfmt --check）
- 格式修复：`pnpm format:fix`（oxfmt --write）
- 术语说明：
  - “gate” 指你当前决策必须通过的验证命令或命令集。
  - 本地开发 gate 是默认快速循环，通常是 `pnpm check` 加你实际需要的定向测试。
  - 落地 gate 是推送 `main` 前更高门槛，通常是 `pnpm check`、`pnpm test`，若改动可能影响构建产物、打包、懒加载/模块边界或已发布接口，还包括 `pnpm build`。
  - CI gate 是对应工作流 lane 强制执行的门槛（例如 `check`、`check-additional`、`build-smoke`、release validation）。
- 本地开发 gate：常规编辑循环优先 `pnpm check`。这样可把仓库架构策略守卫排除在默认本地循环之外。
- CI 架构 gate：`check-additional` 强制执行架构和边界策略守卫，这些守卫有意不放在默认本地循环中。
- 格式 gate：pre-commit 会在 `pnpm check` 前运行 `pnpm format`。若只想本地预检格式，显式运行 `pnpm format`。
- 若需要快速提交循环，`FAST_COMMIT=1 git commit ...` 会跳过 hook 中全仓库 `pnpm format` 和 `pnpm check`；仅在你明确通过其他方式覆盖变更面时使用。
- 测试：`pnpm test`（vitest）；覆盖率：`pnpm test:coverage`
- 生成的 baseline 产物统一放在 `docs/.generated/`。
- 配置 schema 漂移：`pnpm config:docs:gen` / `pnpm config:docs:check`。
- Plugin SDK API 漂移：`pnpm plugin-sdk:api:gen` / `pnpm plugin-sdk:api:check`。
- 若你修改了配置 schema/help 或公开 Plugin SDK 接口，需更新对应 baseline 产物，并保持这两条漂移检查流程在脚本/工作流/文档指引中并列，不要再发明第三套模式。
- 对于小范围改动，优先跑能直接验证改动行为的小范围测试。若没有有意义的定向测试，需明确说明，并用次优但最直接的验证方式。
- `main` 上工作验证模式：
  - 默认模式：`main` 相对稳定。若 pre-commit 已验证当前树，计入覆盖；避免为仪式重复跑相同检查；优先保证落地前 CI/main 绿色。
  - 快速提交模式：`main` 变动快，你有意优化提交回路。优先在最终落地点附近做显式本地验证；中间或追赶提交在本地已跑等价检查的前提下，可接受 `--no-verify`。
- 推送到 `main` 的推荐落地门槛：默认模式下，尽量在最终 rebase/push 前跑 `pnpm check` 与 `pnpm test`。快速提交模式下，在落地前就变更面做本地验证，不要求每个中间提交都重放完整 hook。
- 定向测试用于证明变更本身。`pnpm test` 仍是 `main` 落地默认门槛；定向测试默认不替代全量门槛。
- 硬门槛：若改动可能影响构建产物、打包、懒加载/模块边界或已发布接口，推送 `main` 前必须运行且通过 `pnpm build`。
- 默认规则：不要在 format、lint、type、build 或必需测试失败时落地改动，尤其当失败由改动引起或与改动面高度相关。快速提交模式只改变验证时序，不降低落地前验证并清理变更面的要求。
- 对于小范围改动，若最新 `origin/main` 已有无关失败，应明确说明、报告你跑过的定向测试，并在扩展处理无关问题或带失败落地前先沟通。
- 不要把定向测试当作忽略“可能相关失败”的许可。

## 编码风格与命名约定

- 语言：TypeScript（ESM）。优先严格类型；避免 `any`。
- 格式化/静态检查使用 Oxlint 与 Oxfmt。
- 不要添加 `@ts-nocheck`，默认也不要加行内 lint 抑制。先修根因；仅在代码确实正确、规则无法安全表达、且注释解释原因时保留抑制。
- 不要禁用 `no-explicit-any`；优先真实类型、`unknown` 或窄适配/helper。仅在必要时更新 Oxlint/Oxfmt 配置。
- 在配置、webhook payload、CLI/JSON 输出、持久化 JSON、第三方 API 响应等外部边界，优先 `zod` 或现有 schema helper。
- 当参数形状会改变运行时行为时，优先使用判别联合（discriminated union）。
- 对可恢复运行时决策，优先 `Result<T, E>` 风格结果与封闭错误码联合。
- 日志、CLI 输出、UI 使用人类可读字符串；不要用自由文本字符串作为内部分支判断的事实来源。
- 避免 `?? 0`、空字符串、空对象或魔法字符串哨兵值，以免静默改变运行语义。
- 若在核心逻辑引入新可选字段或可空语义，当值会影响行为时，优先显式联合或专用类型。
- 新运行时代码控制流，不应在可合理使用封闭代码联合时依赖 `error: string` 或 `reason: string` 分支。
- 动态导入护栏：生产路径中不要对同一模块混用 `await import("x")` 与静态 `import ... from "x"`。若需要懒加载，创建专用 `*.runtime.ts` 边界（从 `x` 重新导出），并仅在懒调用方动态导入该边界。
- 动态导入验证：重构触及懒加载/模块边界后，提交前运行 `pnpm build` 并检查 `[INEFFECTIVE_DYNAMIC_IMPORT]` 警告。
- 扩展 SDK 自导入护栏：在扩展包内部，生产文件不要通过 `openclaw/plugin-sdk/<extension>` 导入其自身扩展。内部导入走本地 barrel（如 `./api.ts` 或 `./runtime-api.ts`）；`plugin-sdk/<extension>` 仅作为外部契约。
- 扩展包边界护栏：在内置插件包内，不要使用解析到包根外部的相对导入/导出。若共享代码属于 plugin SDK，请导入 `openclaw/plugin-sdk/<subpath>`，不要通过 `../` 伸到 `src/plugin-sdk/**` 或其他仓库路径。
- 扩展 API 接口规则：`openclaw/plugin-sdk/<subpath>` 是扩展侧 SDK 代码跨包契约的唯一公开入口。扩展需要新接缝时，先增加公开子路径，不要通过相对路径伸入 `src/plugin-sdk/**`。
- 不要通过原型修改共享类行为（`applyPrototypeMixins`、在 `.prototype` 上 `Object.defineProperty`、或导出 `Class.prototype` 做合并）。请使用显式继承/组合（`A extends B extends C`）或 helper 组合，让 TypeScript 可类型检查。
- 若确需此模式，先停下来并获得明确批准；默认做法是拆分/重构为显式类层级并保持成员强类型。
- 在测试中，除非测试明确说明必须这样做，否则优先实例级 stub，而不是原型级 patch（`SomeClass.prototype.method = ...`）。
- 对复杂或不明显逻辑添加简短代码注释。
- 文件保持简洁；通过提取 helper 代替 “V2” 拷贝。CLI 选项与 `createDefaultDeps` 依赖注入遵循现有模式。
- 目标是把文件控制在约 700 LOC 以内；这是指导原则而非硬限制。若能提升清晰度或可测试性，应拆分/重构。
- 命名：产品/app/文档标题使用 **OpenClaw**；CLI 命令、包/二进制、路径、配置键使用 `openclaw`。
- 英文书写：代码、注释、文档、UI 字符串统一美式拼写和语法（例如 “color” 而非 “colour”，“behavior” 而非 “behaviour”，“analyze” 而非 “analyse”）。

## 发布 / 安全通告工作流

- 使用 `.agents/skills/openclaw-release-maintainer/SKILL.md` 中的 `$openclaw-release-maintainer` 处理发布命名、版本协同、发布鉴权和基于 changelog 的发布说明流程。
- 使用 `.agents/skills/openclaw-ghsa-maintainer/SKILL.md` 中的 `$openclaw-ghsa-maintainer` 处理 GHSA 通告检查、补丁/发布流程、私有 fork 校验和 GHSA API 校验。
- 即使使用 skill，发布与 publish 仍是需要显式批准的操作。

## 测试指南

- 框架：Vitest + V8 覆盖率阈值（lines/branches/functions/statements 均 70%）。
- 命名：与源文件同名的 `*.test.ts`；e2e 使用 `*.e2e.test.ts`。
- 测试若需要 Anthropic/OpenAI 模型常量示例，优先 `sonnet-4.6` 与 `gpt-5.4`；触达旧 Anthropic/GPT 示例时同步更新。
- 触及逻辑后，推送前运行 `pnpm test`（或 `pnpm test:coverage`）。
- 测试需清理 timer、env、全局变量、mock、socket、临时目录、模块状态，确保 `--isolate=false` 仍保持绿色。
- 测试性能护栏：除非模块状态确实需要，不要在 `beforeEach`/逐测试循环里放 `vi.resetModules()` + `await import(...)`（对重模块影响大）。优先静态导入或一次性 `beforeAll` 导入，再直接重置 mocks/运行时状态。
- 测试性能护栏：扩展包内部生产代码，优先细薄本地接缝（`./api.ts`、`./runtime-api.ts` 或更窄 `*.runtime-api.ts`），而非直接导入 `openclaw/plugin-sdk/*`。本地接缝应保持精炼；只有跨真实包边界或暂无合适本地接缝时才直接导入 `plugin-sdk/*`。
- 测试性能护栏：将 snapshot、迁移、安装、bootstrap 等昂贵运行时兜底工作放到专用 `*.runtime.ts` 边界后，以便测试 mock 接缝，避免意外触发真实工作。
- 测试性能护栏：对于仅导入/运行时包装测试，让 wrapper 保持懒加载。若导出函数可按需导入，不要在模块顶层急加载重验证/bootstrap/运行时模块。
- 未在本聊天获得明确批准前，Agent **不得**修改 baseline、inventory、ignore、snapshot 或 expected-failure 文件来压制失败检查。
- 定向/本地调试使用 wrapper：`pnpm test -- <path-or-filter> [vitest args...]`（例如 `pnpm test -- src/commands/onboard-search.test.ts -t "shows registered plugin providers"`）；不要默认用裸 `pnpm vitest run ...`，因为会绕过 wrapper 配置/profile/pool 路由。
- 测试 workers 不要设置超过 16（已经试过）。
- Vitest 仅使用 `forks`。未在本聊天获显式批准前，不要在配置、wrapper 脚本或默认测试命令中引入/恢复任何非 `forks` pool 或替代执行模式（含 `threads`、`vmThreads`、`vmForks` 及未来非标准变体）。
- 若本地 Vitest 有内存压力，wrapper 现会依据主机能力（CPU、内存区间、当前负载）计算预算。落地/gate 保守覆盖可显式使用：`OPENCLAW_TEST_PROFILE=serial OPENCLAW_TEST_SERIAL_GATEWAY=1 pnpm test`。
- Live 测试（真实密钥）：`OPENCLAW_LIVE_TEST=1 pnpm test:live`（仅 OpenClaw）或 `LIVE=1 pnpm test:live`（含 provider live 测试）。Docker：`pnpm test:docker:live-models`、`pnpm test:docker:live-gateway`。Onboarding Docker E2E：`pnpm test:docker:onboard`。
- `pnpm test:live` 默认静默。保留 `[live]` 进度，抑制 profile/gateway 噪声。完整日志：`OPENCLAW_LIVE_TEST_QUIET=0 pnpm test:live`。
- 完整工具包与覆盖范围：`docs/help/testing.md`。
- Changelog：只记录用户可见变更；不写内部/元信息（版本对齐、appcast 提醒、发布流程）。
- Changelog 位置：在活跃版本块中，把新条目追加到目标段落末尾（`### Changes` 或 `### Fixes`）；不要插在段首。
- Changelog 署名：每行最多一个贡献者提及；优先 `Thanks @author`，同一行不要再写 `by @author`。
- 纯测试新增/修复通常**不需要** changelog 条目，除非改变用户可见行为或用户明确要求。
- 移动端：使用模拟器前，先检查真实连接设备（iOS + Android），优先真机。

## 提交与 Pull Request 指南

- 使用 `.agents/skills/openclaw-pr-maintainer/SKILL.md` 中的 `$openclaw-pr-maintainer` 完成维护者 PR 分诊、评审、关闭、检索、落地流程。
- 其覆盖自动关闭标签、bug 修复证据门槛、GitHub 评论/搜索陷阱、维护者 PR 决策流程。
- 仓库端到端维护者 PR 流程同样使用 `.agents/skills/openclaw-pr-maintainer/SKILL.md` 的 `$openclaw-pr-maintainer`。

- `/landpr` 在全局 Codex prompts（`~/.codex/prompts/landpr.md`）；落地或合并任何 PR 时都必须遵循该 `/landpr` 流程。
- 提交请用 `scripts/committer "<msg>" <file...>`；避免手动 `git add`/`git commit`，以保证暂存范围可控。
- 提交信息保持简洁、面向动作（例如 `CLI: add verbose flag to send`）。
- 相关改动应分组提交；避免把无关重构打包在一起。
- PR 提交模板（规范）：`.github/pull_request_template.md`
- Issue 提交模板（规范）：`.github/ISSUE_TEMPLATE/`

## Git 备注

- 若 `git branch -d/-D <branch>` 被策略阻止，直接删除本地引用：`git update-ref -d refs/heads/<branch>`。
- Agent **不得**在 `main` 上创建或推送 merge commit。若 `main` 前进了，推送前把本地提交 rebase 到最新 `origin/main`。
- 批量关闭/重开 PR 安全规则：若一次关闭会影响超过 5 个 PR，先向用户明确确认，说明准确 PR 数量与目标范围/查询条件。

## 安全与配置提示

- Web provider 凭据存于 `~/.openclaw/credentials/`；若掉线，重新运行 `openclaw login`。
- Pi 会话默认在 `~/.openclaw/sessions/`；基础目录不可配置。
- 环境变量见 `~/.profile`。
- 不要提交或发布真实手机号、视频或线上配置值。文档、测试和示例中使用明显假的占位值。
- 发布流程：实际 runbook 用私有 [maintainer release docs](https://github.com/openclaw/maintainers/blob/main/release/README.md)，公开发布策略看 `docs/reference/RELEASING.md`，维护工作流使用 `$openclaw-release-maintainer`。

## 本地运行 / 平台备注

- 词汇：`makeup` = `mac app`。
- 重品牌/迁移问题或遗留配置/服务警告：运行 `openclaw doctor`（见 `docs/gateway/doctor.md`）。
- Parallels 冒烟、重跑、升级、调试、结果解读，使用 `.agents/skills/openclaw-parallels-smoke/SKILL.md` 的 `$openclaw-parallels-smoke`（覆盖 macOS、Windows、Linux 客户机）。
- macOS Discord 回环深度排障，使用更聚焦的 `.agents/skills/parallels-discord-roundtrip/SKILL.md` companion skill。
- 不要编辑 `node_modules`（全局/Homebrew/npm/git 安装同理）。更新会覆盖。skill 备注写在 `tools.md` 或 `AGENTS.md`。
- 若需要仅本地生效的 `.agents` 忽略，使用 `.git/info/exclude`，不要改仓库 `.gitignore`。
- 在仓库任意位置新增 `AGENTS.md` 时，也要加一个指向它的 `CLAUDE.md` 软链接（示例：`ln -s AGENTS.md CLAUDE.md`）。
- Signal：“update fly” => `fly ssh console -a flawd-bot -C "bash -lc 'cd /data/clawd/openclaw && git pull --rebase origin main'"`，然后 `fly machines restart e825232f34d058 -a flawd-bot`。
- CLI 进度：用 `src/cli/progress.ts`（`osc-progress` + `@clack/prompts` spinner）；不要手搓 spinner/progress bar。
- 状态输出：保持表格 + ANSI 安全换行（`src/terminal/table.ts`）；`status --all` = 只读可粘贴，`status --deep` = 探测。
- Gateway 当前仅作为菜单栏 app 运行；不存在单独 LaunchAgent/helper label。重启请用 OpenClaw Mac app 或 `scripts/restart-mac.sh`；验证/结束请用 `launchctl print gui/$UID | grep openclaw`，不要假设固定 label。**在 macOS 调试时，通过 app 启停 gateway，而不是临时 tmux 会话；交接前清理临时隧道。**
- macOS 日志：用 `./scripts/clawlog.sh` 查询 OpenClaw 子系统 unified logs；支持 follow/tail/category 过滤，且需要对 `/usr/bin/log` 的免密 sudo。
- 若本地有共享 guardrails，请先阅读；否则遵循本仓库指引。
- SwiftUI 状态管理（iOS/macOS）：优先 `Observation` 框架（`@Observable`、`@Bindable`）而非 `ObservableObject`/`@StateObject`；除非兼容性必须，不要新增 `ObservableObject`，并在触达相关代码时迁移旧用法。
- 新增连接 provider 时，更新所有 UI 面和文档（macOS app、web UI、移动端（如适用）、onboarding/overview 文档），并补齐状态与配置表单，确保 provider 列表和设置同步。
- 版本位置：`package.json`（CLI）、`apps/android/app/build.gradle.kts`（versionName/versionCode）、`apps/ios/Sources/Info.plist` + `apps/ios/Tests/Info.plist`（CFBundleShortVersionString/CFBundleVersion）、`apps/macos/Sources/OpenClaw/Resources/Info.plist`（CFBundleShortVersionString/CFBundleVersion）、`docs/install/updating.md`（固定 npm 版本）、以及 Peekaboo Xcode projects/Info.plists（MARKETING_VERSION/CURRENT_PROJECT_VERSION）。
- “Bump version everywhere” 指上述所有版本位置，**不含** `appcast.xml`（仅在切新的 macOS Sparkle 发布时改 appcast）。
- **重启应用：**“restart iOS/Android apps” 指重建（重新编译/安装）并重启，不是仅 kill/launch。
- **设备检查：**测试前先确认已连接真实 iOS/Android 设备，再考虑模拟器/仿真器。
- iOS Team ID 查询：`security find-identity -p codesigning -v` → 使用 Apple Development (…) TEAMID。回退方案：`defaults read com.apple.dt.Xcode IDEProvisioningTeamIdentifiers`。
- A2UI bundle hash：`src/canvas-host/a2ui/.bundle.hash` 自动生成；遇到异常变更可忽略，仅在需要时通过 `pnpm canvas:a2ui:bundle`（或 `scripts/bundle-a2ui.sh`）重生成。提交时把 hash 作为单独 commit。
- 发布签名/notary 凭据在仓库外管理；维护者在私有 [maintainer release docs](https://github.com/openclaw/maintainers/tree/main/release) 保持该配置。
- Lobster 调色板：使用 `src/terminal/palette.ts` 里的共享 CLI 调色板（不要硬编码颜色）；按需应用到 onboarding/config 提示及其他 TTY UI 输出。
- 当被要求打开 “session” 文件时，打开 `~/.openclaw/agents/<agentId>/sessions/*.jsonl` 下的 Pi 会话日志（使用系统提示 Runtime 行中的 `agent=<id>` 值；若未指定取最新），不要打开默认 `sessions.json`。若要看其他机器日志，通过 Tailscale SSH 到对应机器读取同一路径。
- 不要通过 SSH 重建 macOS app；重建必须在 Mac 本机执行。
- Voice wake 转发提示：
  - 命令模板保持 `openclaw-mac agent --message "${text}" --thinking low`；`VoiceWakeForwarder` 已对 `${text}` 做 shell 转义。不要再加额外引号。
  - launchd PATH 很精简；确保 app 的 launch agent PATH 包含标准系统路径和 pnpm bin（通常 `$HOME/Library/pnpm`），以便通过 `openclaw-mac` 调用时能解析 `pnpm`/`openclaw` 二进制。

## 协作 / 安全备注

- 处理 GitHub Issue 或 PR 时，在任务末尾输出完整 URL。
- 回答问题时仅给高置信度结论：在代码中验证，不要猜测。
- 绝不要升级 Carbon 依赖。
- 任何带 `pnpm.patchedDependencies` 的依赖都必须使用精确版本（不能 `^`/`~`）。
- patch 依赖（pnpm patches、overrides、或 vendored 改动）需显式批准；默认不做。
- **多 Agent 安全：**除非明确要求，不要创建/应用/删除 `git stash`（包括 `git pull --rebase --autostash`）。假设其他 agent 正在工作；保持无关 WIP 不变，避免跨面状态改动。
- **多 Agent 安全：**当用户说 “push” 时，你可以 `git pull --rebase` 集成最新变更（不要丢其他 agent 的工作）。当用户说 “commit” 时，仅提交你的改动。当用户说 “commit all” 时，按分组提交全部。
- **多 Agent 安全：**相关工作优先采用分组的 `commit` / `pull --rebase` / `push` 周期，而不是很多碎片同步。
- **多 Agent 安全：**除非明确要求，不要创建/移除/修改 `git worktree` 检出（或编辑 `.worktrees/*`）。
- **多 Agent 安全：**除非明确要求，不要切换分支或 checkout 到其他分支。
- **多 Agent 安全：**允许多 agent 并行，但每个 agent 必须使用各自独立会话。
- **多 Agent 安全：**看到未识别文件时继续工作；聚焦你的改动，仅提交你负责的部分。
- Lint/格式噪声：
  - 若 staged+unstaged diff 仅为格式变更，自动处理，无需询问。
  - 若已请求 commit/push，自动暂存并把仅格式的后续改动并入同一提交（或必要时一个很小的后续提交），无需额外确认。
  - 仅当改动有语义变化（逻辑/数据/行为）时才询问。
- **多 Agent 安全：**报告聚焦你的编辑；除非确实阻塞，避免 guard-rail 式免责声明；当多个 agent 同改一文件且可安全推进时继续；仅在相关时简要提示“存在其他文件”。
- Bug 排查：下结论前先读相关 npm 依赖源码与本地相关代码，目标是高置信度根因。
- 代码风格：对复杂逻辑添加简短注释；尽量保持文件低于 ~500 LOC（必要时拆分/重构）。
- 工具 schema 护栏（google-antigravity）：工具输入 schema 避免 `Type.Union`，不要 `anyOf`/`oneOf`/`allOf`。字符串枚举使用 `stringEnum`/`optionalStringEnum`（Type.Unsafe enum）；可选项用 `Type.Optional(...)` 而非 `... | null`。顶层 schema 保持 `type: "object"` + `properties`。
- 工具 schema 护栏：避免在 tool schema 中直接使用 `format` 属性名；某些校验器把 `format` 当保留关键字并拒绝 schema。
- 不要向外部消息面（WhatsApp、Telegram）发送流式/部分回复；只能发送最终回复。流式/工具事件仍可发送到内部 UI/控制通道。
- 手工使用 `openclaw message send` 且消息包含 `!` 时，使用下文 heredoc 模式，避免 Bash 工具转义问题。
- 发布护栏：未经操作者明确同意，不要改版本号；运行任何 npm publish/release 步骤前必须先征得许可。
- Beta 发布护栏：使用 beta Git tag（例如 `vYYYY.M.D-beta.N`）时，npm 发布也要使用匹配 beta 版本后缀（例如 `YYYY.M.D-beta.N`），不要仅用 plain version + `--tag beta`，否则 plain version 名会被占用/阻塞。
